# Raider.io app key from https://raider.io/settings/apps — never commit the real value.
RIO_ACCESS_KEY=

# Warcraft Logs v2 OAuth client — never commit the real values.
WCL_CLIENT_ID=
WCL_CLIENT_SECRET=
# WCL_RACE_TTL_SECONDS=480

# Warcraft Logs v1 public key (fallback) — never commit the real value.
WCL_API_KEY=

# Same bot tokens as the existing grok bots (copied on Railway, not stored in git).
TELEGRAM_BOT_TOKEN=
TELEGRAM_RWF_CHAT_ID=
DISCORD_BOT_TOKEN=
DISCORD_RWF_CHANNEL_ID=

# Optional
# RWF_POLL_SECONDS=30
# RWF_STATE_PATH=/data/rwf-state.json
# RWF_TW_STATE_PATH=/data/tw-state.json
# RWF_DEST_PATH=/data/rwf-destinations.json
# RWF_TW_DEST_PATH=/data/tw-destinations.json
# RWF_DRY_RUN=0
# RWF_ONCE=0
